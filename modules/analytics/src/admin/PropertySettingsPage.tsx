/**
 * Settings route — renders the property workspace shell, which detects
 * the /settings path suffix and activates the Settings tab. Kept as a
 * separate route component so the deep link stays first-class.
 */
export { default } from './PropertyDashboardPage';
