/**
 * Admin: instance-wide domain governance (allowlist/denylist).
 * Phase 1: stub; Phase 3 implements CRUD.
 */

export default function FetchDomainRulesPage() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-semibold">Web Fetch — Domain Rules</h1>
      <p className="mt-2 text-sm text-gray-600">
        Manage instance-wide allow/deny rules for the fetch surface. Phase 3.
      </p>
    </div>
  );
}
