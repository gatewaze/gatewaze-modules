/**
 * Admin: per-key fetch configuration (quota overrides, domain rules).
 * Phase 1: stub; Phase 3 implements the drawer.
 */

export default function FetchKeyConfigDrawer() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-semibold">Web Fetch — Key Configuration</h1>
      <p className="mt-2 text-sm text-gray-600">
        Per-key quotas and domain allow/deny rules. Phase 3.
      </p>
    </div>
  );
}
