{
  "name": "@premium-gatewaze-modules/prefect",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "main": "index.ts",
  "types": "index.ts",
  "description": "Self-hosted Prefect Server + Python worker that runs the content discovery pipeline and (Phase 3) AI newsletter generation flows. The TypeScript manifest declares config and migrations; the actual worker runtime lives under workers/prefect/.",
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@gatewaze/shared": "*"
  }
}
