// Re-export the existing events page from the admin app
// This will be moved here fully in a subsequent step; for now,
// re-exporting from the core location to avoid a massive file move
export { default } from '@/app/pages/events/index';
